import forcomp.Anagrams
import Anagrams._
sentenceAnagrams(List("arex"))
combinations(wordOccurrences("linrex")).length
val x = wordOccurrences("assessment")
val y = wordOccurrences("assess")
subtract(x, y)
Stream(1,2,3)
val s:Stream[Int] = 1#::s
s.take(5).toList
